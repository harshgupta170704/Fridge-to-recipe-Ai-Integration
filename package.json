{
  "name": "fridge-chef-monorepo",
  "version": "1.0.0",
  "description": "Root package.json for deploying FridgeChef to platforms like Render or Heroku",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --include=dev --prefix client",
    "build:client": "npm run build --prefix client",
    "build": "npm run install:all && npm run build:client",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
